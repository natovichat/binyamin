using System;
using System.Linq;
using System.IO;
using System.IO.IsolatedStorage;
using System.Collections.Generic;
using Microsoft.LightSwitch;
using Microsoft.LightSwitch.Framework.Client;
using Microsoft.LightSwitch.Presentation;
using Microsoft.LightSwitch.Presentation.Extensions;
namespace LightSwitchApplication
{
    public partial class zz
    {
        //partial void zz_InitializeDataWorkspace(List<IDataService> saveChangesTo)
        //{
        //    Boolean has_order;
        //    int i = 0;
        //    VisualCollection<SchoolPart> schools = new VisualCollection<SchoolPart>();
        //    foreach (SchoolPart school in this.SchoolParts)
        //    {
        //        has_order = false;
        //        foreach (Orders order in this.Orders)
        //        {  
        //            if (school.FullName == order.SchoolPart.FullName)
        //            {
        //                has_order = true;
        //            }
        //        }

        //        if (true == has_order)
        //            schools.AddNew(school);
        //        i++;
        //    }

        //}
    }
}
